# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/


# Go workspace file
go.work

.idea/
*.iml

tmp/
tmpFiles/
*.tmp
# Runtime log OUTPUT, at the repo root only. Unanchored `logs/` matched any
# directory named logs at any depth — including the logs PACKAGE, whose
# source was silently dropped from commits while building fine locally.
/logs/*.log
lastupdate.tmp
commentsRouter*.go
/visor
/casvisor
.DS_Store
