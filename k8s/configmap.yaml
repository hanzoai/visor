apiVersion: v1
kind: Secret
metadata:
  name: visor-config
  namespace: hanzo
type: Opaque
stringData:
  app.conf: |
    appname = hanzo-visor
    httpport = 19000
    runmode = prod
    SessionOn = true
    copyrequestbody = true
    driverName = postgres
    # Get password from KMS: kms.hanzo.ai/hanzo/vm/postgres-password
    dataSourceName = "user=hanzo password=<from-kms> dbname=hanzo_vm host=postgres.hanzo.svc port=5432 sslmode=disable"
    dbName = hanzo_vm
    tableNamePrefix =
    isDemoMode = false
    logPostOnly = true
    redisEndpoint =
    guacamoleEndpoint = vmd.hanzo.svc:4822
    iamEndpoint = http://iam.hanzo.svc:8000
    clientId = b108dacba027db36ec26
    # Get from KMS: kms.hanzo.ai/hanzo/vm/iam-client-secret
    clientSecret = <from-kms>
    iamOrganization = "hanzo"
    iamApplication = "app-hanzo-vm"
