apiVersion: v1
kind: Service
metadata:
  name: compute
  namespace: hanzo
  labels:
    app: compute
spec:
  type: ClusterIP
  ports:
    - port: 19000
      targetPort: 19000
      protocol: TCP
  selector:
    app: compute
